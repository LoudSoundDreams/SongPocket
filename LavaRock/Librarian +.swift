// 2024-12-18

import MediaPlayer

extension Librarian {
	static func merge_MediaPlayer_items(
		_ mpAlbums_unsorted: [MPMediaItemCollection]
	) {
		// For each MediaPlayer collection, figure out whether we already have a corresponding `LRAlbum` for it.
		
		// TO DO
		if let the_lrCrate = the_lrCrate {
			remove_lrCrate(the_lrCrate)
		}
		
		
	}
}
